---
# handlers file for kanidm_community.kanidm.kanidm-unixd

- name: "Update apt cache"
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: "Restart kanidm-unixd service"
  ansible.builtin.systemd:
    name: "kanidm-unixd"
    state: restarted
    enabled: true
